The readme is not updated after mid eval, sorry about this.


# Market Regime Detection and Adaptive Trading using Hidden Markov Models

## Problem Statement

The selected period covers multiple economic cycles including prolonged bull markets, corrections, the COVID-19 market crash, post-pandemic recovery, and inflation-driven volatility. Since Hidden Markov Models learn hidden market regimes from historical transitions, exposing the model to diverse market conditions improves the robustness of the learned state transitions.

---

## Project Architecture

Before diving into the implementation details, it is useful to understand how all the different modules of the project interact with each other. The entire pipeline can be divided into six major stages, starting from collecting raw market data and ending with evaluating the trading strategy.

```text
              Historical Market Data
                       │
                       ▼
          Data Collection & Cleaning
                       │
                       ▼
            Feature Engineering
      (Returns, Volatility, Range)
                       │
                       ▼
        Hidden Markov Model (HMM)
                       │
      ┌────────────────┴────────────────┐
      ▼                                 ▼
 Hidden States                 State Probabilities
      │                                 │
      └────────────────┬────────────────┘
                       ▼
              Transition Forecast
                       │
                       ▼
               Strategy Engine
                       │
                       ▼
             Trading Signals & Allocation
                       │
                       ▼
                 Backtesting Engine
                       │
                       ▼
        Performance Evaluation & Analysis
```

---

## Repository Structure

The project is organized into independent modules so that each stage of the pipeline can be developed, tested and improved separately. This modular design also makes it easier to experiment with different HMM configurations and trading strategies without affecting the rest of the project.

```text
.
├── data/
│   ├── clean_data.csv
│   ├── features.csv
│   ├── states.csv
│   ├── state_probabilities.csv
│   ├── transition_matrix.csv
│   ├── state_characteristics.csv
│   ├── persistence_metrics.csv
│   ├── forecast_probabilities.csv
│   ├── signals_strategy_1.csv
│   └── signals_strategy_2.csv
│
├── notebooks/
│   ├── analysis.ipynb
│   ├── backtesting_analysis.ipynb
|   └── base_hmm_model.ipynb
│
├── src/
│   ├── dashboard/
│   ├── data_pipeline/
│   ├── features/
│   ├── hmm/
│   ├── strategy/
│   │   ├── strategy_engine_1.py
│   │   ├── strategy_engine_2.py
│   │   ├── transition_forecast.py
│   │   ├── backtesting.py
│   │   └── strategies/
│   └── validation/
│
├── reports/
├── presentation/
├── PROJECT_PIPELINE.md
└── README.md
```

### Directory Description

#### `data/`

Stores all intermediate datasets generated throughout the pipeline. Every stage of the project produces an output file which is later consumed by the next stage.

Some important files include:

* **`clean_data.csv`** — Cleaned historical market data.
* **`features.csv`** — Engineered features used for HMM training.
* **`states.csv`** — Most likely hidden state for each trading day.
* **`state_probabilities.csv`** — Probability distribution over all hidden states.
* **`transition_matrix.csv`** — Learned transition probabilities between hidden regimes.
* **`forecast_probabilities.csv`** — One-step-ahead regime probabilities generated using the transition matrix.
* **`signals_strategy_1.csv`** and **`signals_strategy_2.csv`** — Trading signals generated by the implemented strategies.

---

#### `notebooks/`

Contains Jupyter notebooks used for experimentation, visualization and exploratory analysis before integrating the code into the main project.

---

#### `src/`

Contains the complete source code of the project.

Each submodule is responsible for one stage of the pipeline:

* **`data_pipeline/`** — Data collection and preprocessing.
* **`features/`** — Feature engineering.
* **`hmm/`** — Hidden Markov Model implementation.
* **`strategy/`** — Transition forecasting, strategy generation and backtesting.
* **`validation/`** — Model validation and future evaluation utilities.
* **`dashboard/`** — Dashboard and visualization modules.

---
## Installation

Follow the steps below to set up the project locally.

### Clone the Repository

```bash
git clone https://github.com/Abhinav-2106/iitisoc-hmm-market-regimes
cd iitisoc-hmm-market-regimes
```

### Create a Virtual Environment

```bash
python -m venv .venv
```

Activate the virtual environment:

**Linux / macOS**

```bash
source .venv/bin/activate
```

**Windows**

```bash
.venv\Scripts\activate
```

### Install the Required Packages

```bash
pip install -r requirements.txt
```

> **Note:** The project has been developed and tested primarily on **Ubuntu Linux** using **Python 3.12**. Other operating systems should also work, provided the required dependencies are installed.
---


## Data Collection and Preproscessing   

**Status** : Completed

### Dataset Selection

We are using NIFTy 50 (^NSEI) as the primary dataset for the HMM training. 

Reasons for selection :

* Represents Indian equity market* COVID crash.
* High Inflation.

### Time Period

We are using 2010-2025.

Because it includes :

* Multiple occurance of all regimes.
* COVID crash.
* High Inflation.

### Data Cleaning

The raw market data downloaded using `yfinance` is cleaned and organized before analysis. The column names are standardized, the Date index is converted into a regular column, unnecessary metadata is removed, and the data is sorted chronologically. The cleaned dataset is then saved as `clean_data.csv` and used for feature engineering and HMM training.

---

## Feature Engineering

**Status** : Under Development (implemented 3 features may increase as per the requirements in the experminets).

Feature engineering is the process of converting raw market prices into meaningful statistical features that can be used by the Hidden Markov Model. Instead of training the HMM directly on prices, we train it on features that describe different aspects of market behaviour.

### Log Returns

We use **log returns** instead of raw price data for training the HMM because they provide a statistically normalized representation of price changes.

Using raw prices is not ideal because the same absolute price movement can have completely different meanings at different price levels. For example, a ₹10 increase from ₹100 to ₹110 represents a **10%** gain, whereas a ₹10 increase from ₹1000 to ₹1010 represents only a **1%** gain. Returns normalize these changes and make them comparable across different price levels.

We use **log returns** instead of simple returns because they are **additive over time**, making them more suitable for time-series modelling. They are also closer to being stationary, which is an important property for statistical models like the Hidden Markov Model.

### Rolling Volatility (20 Days)

Volatility is calculated as the rolling standard deviation of the log returns over a **20-day rolling window**.

This is one of the most important features because market regimes differ not only in the direction of price movement but also in the level of market uncertainty. For example, bullish markets generally exhibit relatively low volatility, whereas bear markets and financial crises are often characterized by sharp increases in volatility.

A **20-day window** is used because it approximately represents one trading month. This provides a good balance between responsiveness and stability. A much smaller window would react to every short-term fluctuation, making the volatility estimate noisy, while a much larger window would react too slowly to changes in market conditions.

### Daily Trading Range

The daily trading range is calculated as:

`(High - Low) / Close`

This feature measures the magnitude of intraday price movement relative to the closing price.

While rolling volatility captures price behaviour across multiple trading sessions, the daily trading range measures how active the market is within a single trading day. Larger trading ranges usually indicate increased market activity, higher uncertainty, or stronger buying and selling pressure.

The engineered features are finally stored in **`features.csv`**, which is used as the observation sequence for training the Hidden Markov Model.

---

## Hidden Markov Model

**Status:** Evolving *(The baseline Gaussian HMM has been implemented. Model selection and validation are currently under investigation.)*

### Why Hidden Markov Models?

Financial markets do not remain in the same condition forever. They continuously switch between bullish, bearish and sideways phases. The problem is that these market regimes are never explicitly labelled—we only observe daily prices.

A Hidden Markov Model (HMM) treats these market conditions as **hidden states** and tries to infer them from observable market features such as returns, volatility and trading range.

The core idea is that market is always in one hidden regime and that regime changes over time according to certain probabilities. 

Since no labelled data is required, HMMs are a natural choice for identifying market regimes using only historical price data.

---

### Why Gaussian HMM?

The features used in this project (log returns, rolling volatility and trading range) are continuous-valued variables.

For this reason, we use the **Gaussian Hidden Markov Model** provided by the `hmmlearn` library. Each hidden regime is assumed to generate observations following a multivariate Gaussian distribution.

This allows every regime to learn its own statistical behaviour, including: average market return, market volatility and intraday trading activities. 

---

### Model Inputs

The model is trained using daily data from the **NIFTY 50 Index**.

The observation sequence consists of three engineered features:

| Feature             | Purpose                                                      |
| ------------------- | ------------------------------------------------------------ |
| Log Returns         | Capture the daily market direction                           |
| Rolling Volatility  | Measure market uncertainty over the previous 20 trading days |
| Daily Trading Range | Measure intraday market activity                             |

Since these features exist on different numerical scales, they are standardized using `StandardScaler` before training. This prevents any single feature from dominating the learning process and improves numerical stability during optimization.

---

### Model Configuration

The current implementation uses the following configuration:

```python
GaussianHMM(
    n_components=6,
    covariance_type="full",
    n_iter=100,
    random_state=42
)
```

Current parameter choices:

* **6 Hidden States** — Currently selected as the baseline configuration.
* **Full Covariance Matrix** — Allows the model to learn correlations between all input features within each regime.
* **100 EM Iterations** — Maximum number of Baum-Welch optimization steps.
* **Random Seed = 42** — Ensures reproducible results.

---

### Model Training

The model is trained using the **Baum-Welch (Expectation-Maximization)** algorithm.

During training, the HMM automatically learns:

* The statistical characteristics of every hidden regime.
* The probability of transitioning between different regimes.
* The initial probability of each regime.

Unlike supervised learning algorithms, the HMM receives **no regime labels** during training. Every hidden state is discovered directly from the historical market data.

---

### Regime Detection

Once the model has been trained, two outputs are generated.

#### Most Probable Hidden State

Using the **Viterbi Algorithm**, the model estimates the single most likely sequence of hidden market regimes throughout the historical data.

This output is stored in:

* `states.csv`

#### Regime Probabilities

Instead of assigning only one state, the model also computes the probability of the market belonging to every hidden regime.

For example,

```text
Bull : 0.72
Sideways : 0.18
Bear : 0.10
```

These probabilities provide a measure of confidence and are later used by the trading strategy to determine portfolio allocation.

The probabilities are stored in:

* `state_probabilities.csv`

---

### Additional Outputs

During training, several additional artifacts are generated:

| File                        | Description                                  |
| --------------------------- | -------------------------------------------- |
| `transition_matrix.csv`     | Probability of moving between hidden regimes |
| `state_characteristics.csv` | Statistical summary of every hidden state    |
| `persistence_metrics.csv`   | Expected duration of every regime            |
| `hmm_model.pkl`             | Saved trained Gaussian HMM                   |
| `scaler.pkl`                | Saved feature scaler                         |

These outputs are later used for regime analysis, transition forecasting and trading strategy development.

---

## Future Improvements

The current implementation serves as the baseline Hidden Markov Model. Several improvements are planned as part of the ongoing research and development.

### Model Selection

* Compare multiple values of `n_components` instead of manually selecting six states.
* Use AIC, BIC and Log-Likelihood to determine the optimal number of hidden regimes.
* Compare different state counts based on trading performance and interpretability.

### Validation

* Remove look-ahead bias from the current implementation.
* Introduce rolling-window and walk-forward model retraining.
* Evaluate the model on out-of-sample data.

### Regime Analysis

* Compute detailed statistics for every hidden state.
* Assign meaningful labels such as Bull, Bear and Sideways using statistical evidence rather than visual inspection.
* Study regime persistence and transition behaviour.

### Feature Engineering

* Experiment with additional market features such as momentum, volume and India VIX.
* Perform feature ablation studies to understand the contribution of each feature.

### Model Comparison

* Compare the Gaussian HMM with more advanced regime-switching models.
* Investigate regime-dependent volatility models such as Markov-Switching GARCH.

### Trading Strategy Integration

* Compare current-state and forecasted-state based trading strategies.
* Benchmark all strategies against a Buy & Hold portfolio using standard risk metrics such as CAGR, Sharpe Ratio and Maximum Drawdown.
* Perform walk-forward validation of the complete trading pipeline.


---

## Strategy Development

**Status:** Evolving *(Basic strategy implementation and backtesting have been completed. The regime-specific strategy is currently under development.)*

The objective of the trading strategy module is to convert the output of the Hidden Markov Model into actionable trading decisions. Throughout the development of this project, multiple versions of the strategy were implemented to improve the way regime information is utilized.

Rather than directly committing to a single approach, each version was designed to overcome the limitations of the previous one.

### Version 1 — Probability-Based Allocation

The first strategy uses the **current regime probabilities** generated by the Hidden Markov Model.

Each hidden state is assigned a **state weight**, representing the percentage of capital that should be allocated if the model is completely confident that the market is in that regime.

For example:

* **Bull State** → 100% allocation
* **Sideways State** → 50% allocation
* **Bear State** → 0% allocation

Since the HMM outputs probabilities rather than a single hard classification, the final portfolio allocation is calculated as the weighted average (dot product) of the state probability vector and the corresponding state weights.

This strategy naturally adjusts the portfolio allocation according to the confidence of the HMM.


### Version 2 — Transition Matrix Forecasting

The second version extends the previous strategy by introducing **one-step regime forecasting**.

Instead of making decisions solely based on today's detected regime, the transition matrix learned by the Hidden Markov Model is used to estimate the probability distribution of the market for the next trading day.

The forecasting process is implemented in **`transition_forecast.py`**.

If the current regime probability vector is

```text
v(t) = [Bull, Bear, Sideways]
```

then the forecasted probability vector is obtained using

```text
v(t+1) = v(t) × T
```

where **T** is the transition matrix learned during HMM training.

The motivation behind this approach was to make the trading strategy proactive rather than purely reactive.

However, during experimentation, it was observed that the forecasted probabilities were very similar to the current probabilities for most trading days. This is mainly because market regimes tend to be highly persistent, resulting in only marginal differences between the two strategies.

Although the improvement in backtesting performance was limited, this experiment validated the behaviour of the Hidden Markov Model and demonstrated that increasing model complexity does not necessarily lead to significantly better trading performance.


### Version 3 — Regime-Specific Trading Strategy

The current version under development focuses on **regime-specific trading logic**.

Instead of applying the same allocation rule under every market condition, the strategy first identifies the prevailing market regime and activates a trading strategy specifically designed for that regime.

The proposed mapping is:

* **Bull Market** → Momentum Strategy
* **Sideways Market** → Mean Reversion Strategy
* **Bear Market** → Cash / Capital Preservation

After selecting the appropriate trading strategy, the regime probabilities are used to determine the final position size.

In this approach:

* The **detected regime** determines **which trading strategy** should be executed.
* The **regime probabilities** determine **how aggressively** capital should be allocated.

This version aims to combine the strengths of statistical regime detection with traditional quantitative trading strategies, producing a more adaptive trading framework than the previous allocation-based approaches.

---
## Backtesting

**Status:** Implemented *(Basic backtesting framework completed.)*

The final step of the pipeline is to evaluate the generated trading strategy on historical market data.

The backtesting engine first merges the generated trading signals with the cleaned NIFTY 50 dataset using the trading date. Daily market returns are then calculated using the closing prices.

To avoid **look-ahead bias**, today's portfolio allocation is applied to the **next trading day** by shifting the target position by one day before calculating the strategy returns.

The cumulative portfolio value (equity curve) is obtained by compounding the daily strategy returns over the entire investment period.

To understand whether the strategy actually adds value, it is also compared against a **Buy & Hold** benchmark over the same time period.

The current implementation reports the following performance metrics:

* Compound Annual Growth Rate (CAGR)
* Sharpe Ratio
* Maximum Drawdown
* Strategy Equity Curve
* Buy & Hold Comparison

These metrics provide a balanced evaluation of both the return and the risk associated with the trading strategy.

### Future Improvements

The current backtesting engine serves as the baseline implementation. Future work includes:

* Incorporating realistic transaction costs and slippage.
* Performing walk-forward and out-of-sample validation.
* Comparing multiple strategy versions using a unified performance table.
* Adding additional risk metrics such as the Sortino Ratio and Calmar Ratio.
* Extending the framework to support multiple financial instruments and portfolio-level backtesting.
---
## Experimental Results

**Status:** Ongoing

### Performance Summary

| Strategy                              |            CAGR |    Sharpe Ratio |    Max Drawdown |
| ------------------------------------- | --------------: | --------------: | --------------: |
| Probability-Weighted Allocation       |       **9.39%** |        **0.92** |     **-15.23%** |
| Transition Matrix Forecast Allocation |       **9.34%** |        **0.93** |     **-14.79%** |
| Buy & Hold                            | *To be updated* | *To be updated* | *To be updated* |

### Key Observations

* Probability-weighted allocation achieved stable risk-adjusted performance with controlled drawdowns.
* Transition matrix forecasting marginally improved the Sharpe Ratio (**0.92 → 0.93**) and reduced Maximum Drawdown (**-15.23% → -14.79%**), while maintaining a similar CAGR.
* The limited improvement indicates that the detected market regimes are highly persistent, making one-step-ahead forecasts similar to the current regime probabilities.
* Both strategies remain conservative during prolonged bull markets, motivating the development of a **Regime-Specific Trading Strategy**.

---
## Visualization and Analytics
